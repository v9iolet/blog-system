<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CommentMapper">

    <resultMap id="commentTree" type="com.blog.entity.Comment">
        <id property="id" column="id"/>
        <result property="articleId" column="article_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="parentId" column="parent_id"/>
        <result property="likes" column="likes"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="userName" column="user_name"/>
        <result property="userAvatar" column="user_avatar"/>
    </resultMap>

    <select id="selectCommentTree" resultMap="commentTree">
        SELECT c.*, u.nickname as user_name, u.avatar as user_avatar
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        WHERE c.article_id = #{articleId} AND c.status = 1
        ORDER BY c.created_at ASC
    </select>
</mapper>
